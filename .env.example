# Railway Frontend Environment Variables Template
# Copy these to your Railway frontend service environment variables

# Optional: If you need to configure the backend API URL
# VITE_API_URL=https://your-auth-server-backend.railway.app

# The PORT variable is automatically set by Railway, don't set this manually
# PORT=3000 (Railway sets this automatically)

# Optional: If you need to configure OIDC or other login providers
# You may need to update the papermerge-runtime-config.js file or
# create environment-specific configs
